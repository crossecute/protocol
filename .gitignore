# Foundry build output: regenerable from source, and large.
contracts/evm/out/
contracts/evm/cache/
contracts/evm/broadcast/

# contracts/evm/lib/ is committed deliberately: CREATE2 parity depends on byte-identical
# initcode, so the exact dependency bytes are load-bearing. forge-std and OZ are pinned
# submodules (docs/todo.md §4); provider SDK files are vendored by contracts/evm/script/vendor/.

# Secrets and local environment
.env
.env.*
*.key
*.pem

# Local editor / tool state
.DS_Store
.vscode/
.idea/
.claude/settings.local.json
